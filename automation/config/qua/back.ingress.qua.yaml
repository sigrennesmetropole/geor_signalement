kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: signalement-back-qua-rm-signalement-back
  namespace: rennes-metropole-georchestra
  labels:
    app.kubernetes.io/instance: signalement-back-qua
    app.kubernetes.io/name: signalement-back-qua
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'true'
    nginx.ingress.kubernetes.io/force-ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/rewrite-target: /signalement/$2
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
spec:
  tls:
    - hosts:
        - >-
          signalement-back-qua.rennes-metropole-georchestra.karbon.open.global
      secretName: signalement-back-qua.rennes-metropole-georchestra.karbon.open.global
  rules:
    - host: signalement-back-qua.rennes-metropole-georchestra.karbon.open.global
      http:
        paths:
          - path: /signalement(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: signalement-back-qua-rm-signalement-back
                port:
                  number: 8080

