kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: signalement-back-dev-rm-signalement-back
  labels:
    app.kubernetes.io/instance: signalement-back-dev
    app.kubernetes.io/name: signalement-back-dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: 'true'
    nginx.ingress.kubernetes.io/force-ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/rewrite-target: /signalement/$2
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
spec:
  tls:
    - hosts:
        - >-
          signalement-back-dev.rennes-metropole-georchestra.karbon.open.global
      secretName: signalement-back-dev.rennes-metropole-georchestra.karbon.open.global
  rules:
    - host: signalement-back-dev.rennes-metropole-georchestra.karbon.open.global
      http:
        paths:
          - path: /signalement(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: signalement-back-dev-rm-signalement-back
                port:
                  number: 8080