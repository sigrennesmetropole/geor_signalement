<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.georchestra</groupId>
	<artifactId>georchestra-signalement-backend</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>georchestra-signalement-backend</name>
	<description>Plugin Georchestra Signalement Backend</description>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- <version>2.2.4.RELEASE</version> -->
		<version>2.1.10.RELEASE</version>
		<relativePath />
	</parent>

	<!-- Modules du projet -->
	<modules>
		<module>georchestra-signalement-api</module>
		<module>georchestra-signalement-service</module>
		<module>georchestra-signalement-core</module>
	</modules>

	<!-- Propriétés du projet -->
	<properties>

		<!-- dependencies version -->
		<commons.io.version>2.6</commons.io.version>
		<commons.lang.version>2.6</commons.lang.version>

		<!-- <httpclient.version>4.5.10</httpclient.version> <json.simple.version>1.1</json.simple.version> 
			<javax.validation.version>2.0.1.Final</javax.validation.version> -->

		<springfox.version>2.8.0</springfox.version>
		<mapstruct.version>1.2.0.Final</mapstruct.version>
		<lombok.version>1.18.6</lombok.version>
		<minidev.version>2.3</minidev.version>

		<activiti.version>7.1.0.M5</activiti.version>
		<!-- <spring-activiti.version>2.2.4.RELEASE</spring-activiti.version> -->

		<swagger.plugin.version>2.4.10</swagger.plugin.version>
		<maven-replacer-plugin.version>1.4.1</maven-replacer-plugin.version>
		<compiler.plugin.version>3.5.1</compiler.plugin.version>
		<assemby.plugin.version>2.6</assemby.plugin.version>
		<ant.plugin.version>1.7</ant.plugin.version>

		<!-- java version -->
		<java.source.version>11</java.source.version>
		<java.target.version>11</java.target.version>

		<!-- Project properties -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

		<!-- sonar properties -->
		<sonar.projectKey>${env.SONAR_KEY}${project.artifactId}</sonar.projectKey>
		<sonar.projectName>${project.artifactId}</sonar.projectName>
		<sonar.exclusions>**/config/*.java, **/exception/**/*.java,
			**/test/*.java</sonar.exclusions>
		<sonar.global.test.exclusions>**/*Test.java</sonar.global.test.exclusions>

	</properties>

	<distributionManagement>
		<repository>
			<id>open.nexus</id>
			<url>${MVN_RELEASES}</url>
		</repository>
		<snapshotRepository>
			<id>open.nexus</id>
			<url>${MVN_SNAPSHOTS}</url>
		</snapshotRepository>
	</distributionManagement>

	<!-- <repositories> <repository> <id>open.nexus</id> <url>${MVN_GROUP}</url> 
		</repository> </repositories> -->
		
	<profiles>
		<profile>
			<id>dev</id>
			<properties>
				<georchestra.addon.deployDir>C:\Program Files\Apache\apache-tomcat-9.0.26\georchestra\mapfishapp\addons</georchestra.addon.deployDir>
			</properties>
		</profile>
	</profiles>

	<dependencyManagement>
		<dependencies>

			<!-- Modules -->
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>georchestra-signalement-api</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>georchestra-signalement-service</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>${project.groupId}</groupId>
				<artifactId>georchestra-signalement-core</artifactId>
				<version>${project.version}</version>
			</dependency>

			<!-- Commons dependencies -->
			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons.io.version}</version>
			</dependency>

			<dependency>
				<groupId>commons-lang</groupId>
				<artifactId>commons-lang</artifactId>
				<version>${commons.lang.version}</version>
			</dependency>

			<!-- json -->
			<dependency>
				<groupId>net.minidev</groupId>
				<artifactId>json-smart</artifactId>
				<version>${minidev.version}</version>
			</dependency>

			<!-- Swagger dependencies -->
			<dependency>
				<groupId>io.springfox</groupId>
				<artifactId>springfox-swagger-ui</artifactId>
				<version>${springfox.version}</version>
			</dependency>

			<dependency>
				<groupId>io.springfox</groupId>
				<artifactId>springfox-swagger2</artifactId>
				<version>${springfox.version}</version>
			</dependency>

			<!-- Map Struct -->
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct-jdk8</artifactId>
				<version>${mapstruct.version}</version>
			</dependency>

			<!-- Activiti -->
			<!-- https://mvnrepository.com/artifact/org.activiti/activiti-spring-boot-starter -->
			<dependency>
				<groupId>org.activiti</groupId>
				<artifactId>activiti-spring-boot-starter</artifactId>
				<version>${activiti.version}</version>
			</dependency>

			<!-- https://mvnrepository.com/artifact/org.activiti/activiti-api-web-examples -->
			<!--<dependency> <groupId>org.activiti</groupId> <artifactId>activiti-api-web-examples</artifactId> 
				<version>7.1.142</version> </dependency> -->

			<dependency>
				<groupId>org.activiti.dependencies</groupId>
				<artifactId>activiti-dependencies</artifactId>
				<version>${activiti.version}</version>
				<scope>import</scope>
				<type>pom</type>
			</dependency>

			<dependency>
				<groupId>org.activiti.cloud.rb</groupId>
				<artifactId>activiti-cloud-runtime-bundle-dependencies</artifactId>
				<version>${activiti.version}</version>
				<scope>import</scope>
				<type>pom</type>
			</dependency>

			<!-- https://mvnrepository.com/artifact/com.google.code.maven-replacer-plugin/maven-replacer-plugin -->
			<dependency>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>maven-replacer-plugin</artifactId>
				<version>${maven-replacer-plugin.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>

				<!-- Swagger -->
				<plugin>
					<groupId>io.swagger</groupId>
					<artifactId>swagger-codegen-maven-plugin</artifactId>
					<version>${swagger.plugin.version}</version>
				</plugin>

				<!-- Generate Lombok annotation then Mapstruct classes -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${compiler.plugin.version}</version>
					<configuration>
						<source>${maven.compiler.source}</source>
						<target>${maven.compiler.target}</target>
						<showDeprecation>true</showDeprecation>
						<showWarnings>true</showWarnings>
						<encoding>${project.build.sourceEncoding}</encoding>
						<annotationProcessorPaths>
							<path>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
								<version>${lombok.version}</version>
							</path>
							<path>
								<groupId>org.mapstruct</groupId>
								<artifactId>mapstruct-processor</artifactId>
								<version>${mapstruct.version}</version>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>

				<plugin>
					<artifactId>maven-assembly-plugin</artifactId>
					<version>${assemby.plugin.version}</version>
				</plugin>

				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-antrun-plugin</artifactId>
					<version>${ant.plugin.version}</version>
				</plugin>

			</plugins>
		</pluginManagement>
	</build>

</project>
